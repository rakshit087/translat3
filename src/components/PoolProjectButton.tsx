import { Button } from "@chakra-ui/react";

export const PoolProjectButton = () => {
  return (
    <Button colorScheme={"purple"} variant="solid" w="100%" rounded={"xl"}>
      Pool Now
    </Button>
  );
};
