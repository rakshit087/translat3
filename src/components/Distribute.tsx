import { Button } from "@chakra-ui/react";

export const Distribute = () => {
  return (
    <Button w={"100%"} colorScheme="purple" variant="solid" size={"sm"} rounded="2xl" margin={0} onClick={() => {}}>
      Distribute
    </Button>
  );
};
